import React, { FunctionComponent } from 'react'

import { stitches } from 'src/shared/ui'

const Container = stitches.styled('div', {

})

export const KnobsPanel: FunctionComponent = () => (
  <Container>
    Knobs
  </Container>
)
